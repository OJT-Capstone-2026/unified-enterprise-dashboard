// Integration test placeholder
// Run the app in a browser and verify routes load correctly

export const routes = ['/', '/portfolio', '/quiz', '/expense', '/news', '/github', '/kanban', '/settings'];

console.log('Integration tests require browser environment.');
console.log('Routes to verify:', routes.join(', '));
